// Import the server from the service directory
const server = require('./service/server');

// The server is already listening for connections as it's initialized in server.js
console.log('Server started via index.js');